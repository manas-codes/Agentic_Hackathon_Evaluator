# CAG 101 Innovation Ideas — Evaluation Framework
# Python 3.11
#
# This file is what the PORTAL needs, and nothing more. It is the file the
# hosting platform installs, so every line here is installed on every deploy.
# The extraction and scoring pipeline needs PyMuPDF, scikit-learn and the rest
# — those live in requirements-pipeline.txt and are not installed on the host,
# because the pipeline does not run there. It runs on an operator machine that
# has the submission files and an API credential.
#
# Local development, full stack:
#   pip install -r requirements.txt -r requirements-pipeline.txt

# --- Web portal ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9
itsdangerous>=2.1.0      # signed session cookies
argon2-cffi>=23.1.0      # password hashing

# --- Data / persistence ---
SQLAlchemy>=2.0.0
pydantic>=2.0.0
PyYAML>=6.0
psycopg[binary]>=3.2     # Postgres. Used whenever DATABASE_URL is set.

# --- Export ---
openpyxl>=3.1.0          # the portal generates the workbook on request

# --- Config ---
python-dotenv>=1.0.0
